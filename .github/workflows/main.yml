name: Atualizar atividade recente on: schedule: - cron: '0 */6 * * *' workflow_dispatch: jobs: build: runs-on: ubuntu-latest steps: - uses: jamesgeorge007/github-activity-readme@master env: GH_TOKEN: ${{ secrets.GH_TOKEN }}
